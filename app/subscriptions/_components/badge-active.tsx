import { Badge } from "@/app/_components/ui/badge";

const BadgeActive = () => {
  return (
    <Badge className="absolute top-10 left-4 bg-primary/10 text-primary text-sm">
      Ativo
    </Badge>
  );
};

export default BadgeActive;
